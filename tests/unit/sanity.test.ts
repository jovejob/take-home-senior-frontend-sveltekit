import { describe, expect, it } from 'vitest';

// Placeholder — will be replaced by the real unit tests required by the brief:
// 1 composite component test + 1 business-logic test (URL state codec), min 5 total.
describe('sanity', () => {
	it('test runner is wired up', () => {
		expect(1 + 1).toBe(2);
	});
});
